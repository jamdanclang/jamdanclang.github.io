{% extends "_base.html" %}

{% block content %}
<div class="container">
  <div class="jumbotron">
    <h1>{{ headline }}</h1>
    
	<div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Year</th>
                  <th>Avg Cost In-State</th>
                  <th>Avg Cost Out of State</th>
                  <th>Avg Cost In-State + Inflation</th>
                  <th>Avg Cost Out-State + Inflation </th>
                </tr>
</thead>
{% for row in history %}
<tbody>
                <tr>
                  <td> {{row.YEAR }} </td>
                  <td> {{ row.AvgCostWithFeesRes }}</td>
                  <td> {{ row.AvgCostWithFeesNonRes }}</td>
                  <td>{{ row. AvgCostWithFeesResINFADJ }}</td>
                  <td> {{row. AvgCostWithFeesNonResINFADJ }}</td>
                </tr>
	</tbody>
{% endfor %}
              	</table>
</div>


  </div>
</div>
<img src="informationunl.png|safe" />




<!--  
<div class="container">
  <div class="jumbotron">
    <h1>{{ second }}</h1>
    
	<div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Institution</th>
                  <th>In-State Costs</th>
                  <th>Out of State Costs</th>
                  <th>Graduation Rates</th>
                  </tr>
</thead>
{% for row in bigten %}
<tbody>
                <tr>
                  <td> {{row.Institution }} </td>
                  <td> {{ row.instate }}</td>
                  <td> {{ row.out }}</td>
                  <td>{{ row.Graduation }}</td>
                  </tr>
	</tbody>
{% endfor %}
              	</table>
</div>

  </div>
</div>
-->
{% endblock content %}

